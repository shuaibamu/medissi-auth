# MediSSI-Auth — Python dependencies (Stage 1)
# Pinned versions per handbook §1.1. Do NOT upgrade without testing.
# Re-freeze with: pip freeze > requirements.txt

# --- Core scientific stack ---
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.1
seaborn==0.13.2

# --- Web/API framework (used by Stage 6 SSI agents) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.27.0

# --- Blockchain interaction ---
web3==6.20.1
eth-account==0.11.3

# --- Cryptography ---
cryptography==42.0.8
pynacl==1.5.0
pycryptodome==3.20.0

# --- IPFS client ---
# We use Filebase (S3-compatible) per project decision; boto3 is sufficient.
# If you choose local Kubo instead, also install: ipfshttpclient==0.8.0a2
boto3==1.34.131
botocore==1.34.131

# --- FHIR parsing (Stage 2) ---
fhir.resources==7.1.0

# --- Data storage (Stage 2) ---
pyarrow==16.1.0       # parquet
python-dotenv==1.0.1  # .env loading

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# --- Notebook (optional, for EDA in Stage 2) ---
jupyterlab==4.2.2
ipykernel==6.29.4
